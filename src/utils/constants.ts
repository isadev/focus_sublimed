export enum ENVIRONMENTS {
    TESTING = 'testing',
    DEVELOP = 'develop',
    PRODUCTION = 'production',
}

export const DATABASE_CONNECTION_PROVIDER = 'DATABASE_CONNECTION';

export enum MODELS_NAME_PROVIDER {
    USER_MODEL = 'USER_MODEL',
}
